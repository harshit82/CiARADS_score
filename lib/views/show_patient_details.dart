import 'package:flutter/material.dart';

class ShowPatientDetails extends StatelessWidget {
  const ShowPatientDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
